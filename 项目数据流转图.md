# 项目数据流转图

本文基于当前 `main` 分支代码梳理，覆盖前端页面、后端 API、素材解析、LLM 生成、项目存储与导出链路。

## 1. 总体数据流

```mermaid
flowchart LR
    U["用户"] --> FE["前端页面<br/>frontend/src/runtime-bundle.js<br/>frontend/src/main.tsx"]

    FE -->|项目信息/内容单元/风格/选择项| API["FastAPI 接口层<br/>backend/main.py"]
    FE -->|上传素材| API
    FE -->|流式生成预览/提案版/完整版| API
    FE -->|导出 Markdown / Word| API

    API --> STORE["项目存储<br/>backend/storage.py"]
    API --> ASSET["素材解析<br/>backend/assets.py"]
    API --> STORY["分镜生成与导出<br/>backend/storyboard.py"]

    STORE --> MODEL["数据模型<br/>backend/models.py"]
    ASSET --> MODEL
    STORY --> MODEL

    STORE --> PJ["outputs/{project_id}/project.json"]
    ASSET --> AF["outputs/{project_id}/assets/*"]
    STORY --> EXP["outputs/{project_id}/*.md / *.docx"]

    ASSET -->|PDF/Word 文本摘要<br/>图片/视频视觉摘要| VISION["视觉/文本模型<br/>OpenAI Compatible API"]
    STORY -->|创作预览/提案版/完整版/步骤生成| LLM["LLM<br/>OpenAI Compatible API"]

    LLM --> STORY
    VISION --> ASSET
    API -->|SSE data: delta/done/error| FE
    API -->|FileResponse 下载| U
```

## 2. 项目创建与状态保存

```mermaid
sequenceDiagram
    participant User as 用户
    participant FE as 前端
    participant API as backend/main.py
    participant Store as backend/storage.py
    participant File as outputs/{project_id}/project.json

    User->>FE: 填写项目名称、客户类型、片型、时长、比例
    FE->>API: POST /api/projects
    API->>Store: save_project(project)
    Store->>Store: ensure_project() 补齐 id / updated_at
    Store->>File: 写入 project.json
    API-->>FE: 返回 ProjectState

    User->>FE: 切换历史项目
    FE->>API: GET /api/projects/{project_id}
    API->>Store: load_project(project_id)
    Store->>File: 读取 project.json
    Store->>Store: sync_asset_message()
    API-->>FE: 返回 ProjectState
```

## 3. 素材上传与解析流

```mermaid
flowchart TD
    FE["前端素材上传面板<br/>main.tsx 增强 reference / brand 字段"] --> UP["POST /api/projects/{id}/assets/upload"]
    UP --> LOAD["load_project()<br/>不存在则新建 ProjectState"]
    LOAD --> UA["upload_asset()"]
    UA --> VALID["校验类型/大小/数量<br/>PDF/Image/Video/Word"]
    VALID --> WRITE["保存原文件<br/>outputs/{id}/assets/{asset_id}_name.ext"]
    WRITE --> KIND{"文件类型"}

    KIND -->|PDF| PDF["parse_pdf()<br/>pypdf 提取文本"]
    KIND -->|Word| WORD["parse_word()<br/>python-docx 提取段落/表格"]
    KIND -->|Image| IMG["parse_image()<br/>PIL 校验图片"]
    KIND -->|Video| VID["parse_video()<br/>OpenCV 抽关键帧"]

    PDF --> TEXTSUM["summarize_text()"]
    WORD --> TEXTSUM
    IMG --> IMGSUM["summarize_images()"]
    VID --> FRAMES["临时 frame_*.jpg"]
    FRAMES --> IMGSUM

    TEXTSUM --> VISION["vision_client()<br/>VISION_API_KEY 或 LLM_API_KEY"]
    IMGSUM --> VISION
    VISION --> ASSET["ProjectAsset.summary<br/>ProjectAsset.extracted_text"]
    ASSET --> ASSETJSON["assets/{asset_id}.json"]
    ASSET --> PROJECT["project.assets 更新"]
    PROJECT --> SAVE["save_project(project)"]
    SAVE --> FE2["返回 AssetUploadResponse"]
```

## 4. 分镜生成流

```mermaid
flowchart TD
    FE["前端确认流程"] --> PREVIEW["/api/generate/preview/stream"]
    FE --> STEP["/api/generate/step"]
    FE --> PROPOSAL["/api/generate/proposal/stream"]
    FE --> FULL["/api/generate/full/stream"]

    PREVIEW --> BPV["build_creative_preview_prompt(project)"]
    STEP --> BP["build_prompt(project, step_key)"]
    PROPOSAL --> BPP["build_proposal_document_prompt(project)"]
    FULL --> BFP["build_full_document_prompt(project)"]

    BPV --> BRIEF["project_brief(project)<br/>meta + content_units + assets"]
    BP --> BRIEF
    BPP --> BRIEF
    BFP --> BRIEF

    BRIEF --> CTX["confirmed_context()<br/>revision_context()<br/>proposal_markdown"]
    CTX --> LLM["OpenAI chat.completions<br/>stream=True 或普通调用"]

    LLM -->|delta| SSE["StreamingResponse<br/>data: delta"]
    LLM -->|done| UPDATE["更新 ProjectState"]

    UPDATE -->|preview| PREVSTEP["steps['preview']"]
    UPDATE -->|step| STEPRESULT["steps[step_key]"]
    UPDATE -->|proposal| PMD["proposal_markdown + active_version=proposal"]
    UPDATE -->|full| FMD["full_markdown/final_markdown + active_version=full"]

    PREVSTEP --> SAVE["save_project(project)"]
    STEPRESULT --> SAVE
    PMD --> SAVE
    FMD --> SAVE
    SAVE --> FE2["前端渲染消息/文档"]
```

## 5. 文档导出流

```mermaid
flowchart LR
    FE["前端导出按钮"] --> API["POST /api/export/{version}/{kind}"]
    API --> KIND{"kind"}

    KIND -->|markdown| WM["write_markdown()"]
    KIND -->|docx| WD["write_docx()"]

    WM --> ASM["assemble_markdown()<br/>无 override 时组装 meta/content_units/steps"]
    ASM --> MDFILE["写入 outputs/{id}/{project}_{version}.md"]

    WD --> WM2["先 write_markdown()"]
    WM2 --> SCRIPT["scripts/generate_docx.py"]
    SCRIPT --> DOCX["写入 outputs/{id}/{project}_{version}.docx"]

    MDFILE --> EXP["project.exports[version_kind]"]
    DOCX --> EXP
    EXP --> SAVE["save_project(project)"]
    SAVE --> URL["返回 download_url"]
    URL --> DL["GET /api/download/{id}/export/{version}/{kind}"]
    DL --> FILE["FileResponse"]
```

## 6. 用户交互确认流程

这部分是当前产品里需要用户参与补齐、选择或确认的流程，主要由 `frontend/src/runtime-bundle.js` 中的步骤状态控制，后端负责保存项目状态、生成预览/文档、导出文件。

```mermaid
flowchart TD
    START["新建/打开项目"] --> INFO["1. 补齐项目基础信息<br/>项目名称、客户类型、影片类型、时长、比例"]
    INFO --> SAVE1["POST /api/projects<br/>保存 ProjectState.meta"]

    SAVE1 --> UNITS["2. 补齐内容单元<br/>可选模板或自由输入"]
    UNITS --> SAVE2["content_units 写入 ProjectState"]

    SAVE2 --> STYLE["3. 询问风格基调<br/>年轻创意 / 商业质感 / 科技感 / 纪实 / 政务 / 电影感"]
    STYLE --> PREF["补充偏好<br/>节奏、口吻、禁忌表达等"]
    PREF --> SAVE3["meta.style + messages 保存"]

    SAVE3 --> MATERIAL["4. 补齐素材与参考信息"]
    MATERIAL --> REF["参考样片<br/>文字描述或上传视频"]
    MATERIAL --> BRAND["品牌资产<br/>Logo / VI / 图片 / PDF / Word"]
    REF --> ASSET["POST /assets/upload<br/>自动解析素材摘要"]
    BRAND --> ASSET
    ASSET --> SYNC["素材摘要回填到表单文本域<br/>reference_samples / brand_assets"]

    SYNC --> PREVIEW_READY["5. 生成前确认<br/>提示用户先生成创作预览"]
    PREVIEW_READY --> PREVIEW["/api/generate/preview/stream<br/>生成主题、口号、内容逻辑、确认项"]

    PREVIEW --> SLOGAN["6. 用户确认口号<br/>使用主推 / 选择备选 / 自定义意见"]
    SLOGAN --> CREATIVE["7. 用户确认整体创意方向<br/>沿用预览 / 更直接 / 更大胆 / 更商业 / 更克制 / 自定义"]
    CREATIVE --> SCALE["8. 用户确认成片套餐/表达尺度<br/>标准完整分镜 / 声音设计 / 难点预警 / Meta 叙事 / 克制表达"]

    SCALE --> OUTPUT["9. 用户确认输出方式"]
    OUTPUT -->|先提案版| PROPOSAL_READY["进入提案版生成节点"]
    OUTPUT -->|直接完整版| FULL_READY["进入完整版生成节点"]

    PROPOSAL_READY --> PROPOSAL["/api/generate/proposal/stream"]
    PROPOSAL --> PROPOSAL_REVIEW["10. 用户审阅提案版"]
    PROPOSAL_REVIEW -->|满意| FULL_READY
    PROPOSAL_REVIEW -->|不满意| PROPOSAL_REV["填写提案版修改方向与具体意见"]
    PROPOSAL_REV --> PROPOSAL

    FULL_READY --> FULL["/api/generate/full/stream"]
    FULL --> FULL_REVIEW["11. 用户审阅完整版"]
    FULL_REVIEW -->|导出| EXPORT["导出 MD / Word"]
    FULL_REVIEW -->|回滚修改| FULL_REV["填写完整版修改方向与具体意见"]
    FULL_REV -->|回到提案版| PROPOSAL
    FULL_REV -->|重生成完整版| FULL
```

### 交互节点说明

| 阶段 | 用户输入/选择 | 写入位置 | 后续影响 |
|---|---|---|---|
| 项目基础信息 | 项目名称、客户类型、影片类型、时长、比例 | `ProjectState.meta` | 决定生成提示词中的项目定位与成片规格 |
| 内容单元 | 模板或自由输入的业务/产品/场景条目 | `ProjectState.content_units` | 决定分镜结构与镜头内容组织 |
| 风格基调 | 风格下拉选择与补充偏好 | `ProjectState.meta.style`、`messages` | 影响文案语气、画面风格、节奏判断 |
| 参考样片 | 文本描述或视频上传 | `meta.reference_samples`、`assets` | 影响参考风格、节奏、镜头语言 |
| 品牌资产 | 文本描述或图片/PDF/Word 上传 | `meta.brand_assets`、`assets` | 影响品牌口径、视觉元素、可用素材 |
| 创作预览 | 用户请求生成预览 | `steps["preview"]` | 先确认主题、口号、内容逻辑，避免直接生成完整稿 |
| 口号确认 | 主推口号/备选口号/自定义意见 | `steps["selection"]` | 进入提案版/完整版时作为用户最终选择 |
| 创意方向确认 | 沿用或调整整体创意 | `steps["selection"]` | 决定叙事策略和视觉表达尺度 |
| 输出方式确认 | 先提案版或直接完整版 | `active_version`、阶段状态 | 决定调用提案版还是完整版生成接口 |
| 修改反馈 | 提案版/完整版修改方向和具体意见 | `steps["revision"]` | 重生成时优先执行最新修改意见 |

## 7. 核心数据对象

```mermaid
classDiagram
    class ProjectState {
      id
      meta
      content_units
      steps
      messages
      stage
      selection_state
      active_version
      proposal_markdown
      full_markdown
      final_markdown
      exports
      assets
      created_at
      updated_at
    }

    class ProjectMeta {
      project_name
      client_type
      video_type
      duration
      aspect_ratio
      style
      version
      brand_assets
      reference_samples
    }

    class ProjectAsset {
      id
      filename
      original_filename
      asset_type
      path
      summary
      extracted_text
      status
      error
    }

    class StepResult {
      key
      title
      content
      confirmed
      updated_at
    }

    ProjectState --> ProjectMeta
    ProjectState --> ContentUnit
    ProjectState --> ProjectAsset
    ProjectState --> StepResult
    ProjectState --> ChatMessage
```

## 8. 当前版本要点

- 单一项目状态文件是 `outputs/{project_id}/project.json`。
- 当前 `main` 分支没有 `backend/chat_flow.py` 对话编排入口，生成主要由 `backend/storyboard.py` 提供。
- 前端主业务来自 `frontend/src/runtime-bundle.js`，`frontend/src/main.tsx` 主要做页面增强：主题切换、侧边栏用户区、状态提示、素材上传面板。
- 素材会先被转成 `summary` / `extracted_text`，再作为 `project_brief()` 的一部分进入后续分镜生成提示词。
- 提案版和完整版分别落在 `proposal_markdown`、`full_markdown/final_markdown`，导出路径记录在 `project.exports`。
